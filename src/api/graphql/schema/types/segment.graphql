type Segment {
  createdDate: String!
  id: ID!
  longDescription: String!
  name: String!
  segmentType: SegmentType!
  shortDescription: String!
}

type Segments {
  meta: PagingMeta!
  objects: [Segment]!
}
