#import "../interfaces/paging.graphql"

type ReturnPolicy {
  longDescription: String!
  name: String!
  shortDescription: String!
}

type ReturnPolicies implements PagedResource {
  objects: [ReturnPolicy]!
}
