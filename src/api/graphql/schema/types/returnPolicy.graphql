type ReturnPolicy {
  longDescription: String!
  name: String!
  shortDescription: String!
}

type ReturnPolicyEdge {
  cursor: String!
  node: ReturnPolicy
}

type PaginatedReturnPolicies {
  edges: [ReturnPolicyEdge]
  pageInfo: PageInfo
}
