type ProductContext {
  baseSort: Int!
  boutiqueContext: String!
  lowInventoryLimit: Int!
  maxPerCart: Int!
  pk: ID!
  product: String!
  productGroupContext: String
  skus: [String]!
}

type ProductContexts {
  meta: PagingMeta!
  objects: [ProductContext]!
}
