type BaseSort {
  bigDoor: Boolean!
  boutiqueContext: String!
  boutiqueName: String!
  boutiqueStart: String!
  dayPart: String!
  segment: String!
  sort: Int!
}

type BaseSortSet {
  meta: PagingMeta!
  objects: [BaseSort]!
}
